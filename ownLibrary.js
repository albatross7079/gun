function hasCollided(wall1,bullet1) {
    bulletEightEdge = bullet1.x+bullet1.width;
    wallLeftEdge = wall1.x;
  }
    if(bulletRightEdge>=ballLeftEdge) {
    return true
  }
  return false;
